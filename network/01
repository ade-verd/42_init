# NETWORK - 01
# Q: Récupérez la liste des interfaces réseau de la machine sans affcher aucun détail
#    pour ces interfaces. Vraiment juste la liste des noms.
# R: Commande

#Darwin
 ifconfig -l

#Linux
# ifconfig -a | cut -d " "  -f 1 | grep .
#or
# ls /sys/class/net
