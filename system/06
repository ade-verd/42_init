# SYSTEM - 06 (sur VM Debian)
# Q: Donnez une commande pour redémarrer le service SSH
# R: Commande

systemctl restart ssh.service 

#or 
# service ssh restart
# verify with: 	service ssh status | grep since
# or: 			systemctl status ssh.service | grep since